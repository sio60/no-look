# ===========================================
# Test 환경 설정
# ===========================================
spring.application.name=backend-test

# VideoEngine 비활성화 (카메라 없는 환경)
video.engine.enabled=false

# 기본 설정값 (테스트용)
video.fps=30
video.transition.default-ms=300
obs.websocket.host=localhost
obs.websocket.port=4455
obs.websocket.password=test
obs.source.name=TestSource
python.socket.port=5050
